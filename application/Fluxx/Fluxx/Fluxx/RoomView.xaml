<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fluxx.RoomView"
             HeightRequest="80"
             BackgroundColor="Black"
             HorizontalOptions="FillAndExpand">
    <ContentView.Content>
        <StackLayout Orientation="Vertical"
                     Margin="5"
                     BackgroundColor="Beige"
                     VerticalOptions="StartAndExpand"
                     HorizontalOptions="FillAndExpand">
            <Label x:Name="lbl_info" TextColor="Red"></Label>
            <StackLayout VerticalOptions="Start"
                         Orientation="Horizontal">
                <Label x:Name="RoomName" Text="unknow"  HorizontalOptions="StartAndExpand" />
                <Label x:Name="RoomNumPlayer"  Text="unknow" HorizontalOptions="End" />
            </StackLayout>
            <StackLayout VerticalOptions="End"
                         x:Name="secondLine"
                         Orientation="Horizontal">
                <Entry x:Name="password" IsPassword="True" Placeholder="Password" Text="" HorizontalOptions="StartAndExpand"/>
                
            </StackLayout>
        </StackLayout>
    </ContentView.Content>
</ContentView>